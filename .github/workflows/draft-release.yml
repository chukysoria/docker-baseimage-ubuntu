name: 'Release Drafter'

on:
  push:
    branches:
      - main
      - master
      - jammy
      - noble

jobs:
  update_release_draft:
    name: Release
    uses: chukysoria/workflows/.github/workflows/new-release.yml@512ec689f489f3ee7c880bdb8a0b8618f5b00be4 # v1.1.20
    with:
      tag-external-version: true
