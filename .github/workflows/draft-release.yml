name: 'Release Drafter'

on:
  push:
    branches:
      - main
      - master
      - jammy
      - noble

jobs:
  update_release_draft:
    name: Release
    uses: chukysoria/workflows/.github/workflows/new-release.yml@cd870671c263b80aec39e2332cbbc02cef9287ad # v1.1.19
    with:
      tag-external-version: true
