name: 'Release Drafter'

on:
  push:
    branches:
      - main
      - master
      - jammy

jobs:
  update_release_draft:
    name: Release
    uses: chukysoria/workflows/.github/workflows/new-release.yml@v1.0.13
    with:
      tag-external-version: true
